READ    this file summarizes the working contract; BP v2
        governs (techne.coop/commons/bp/). read README.md and
        docs/ before any code. the authority map is law here:
        techne.coop/commons/authority-map/

STAND   you are a session-scoped instrument: no memory, no
        standing, no authority. organizers decide and adopt.
        this repo builds a UI onto the live CIS; it holds no
        truth of its own and changes no schema. a feature that
        needs a new permission or a new table is a stop card,
        filed as an issue on techne.co-op/techne.coop, never
        built here first.
        primary build contact: Todd Youngblood (steward).

REPO    github.com/Techne-Co-op/open-world  --  PWA for the
        Open World Participation Framework. same information
        system as techne.coop/intranet, distinct optimized
        UI/UX. app shell may cache; the record never does:
        the halt wins over offline (issue #253).

WORK    one concern per branch. commits carry your authorship
        trailer, e.g. "Authored-by: build-agent / open-world".
        cite the issue you work from.

STOP    schema, permissions, money, membership, governance,
        push notification or any agent-initiated contact,
        new dependencies, public names, store presence:
        stop and ask rather than invent. file the stop card:
        standing-in / found / the question / a default.

MARK    drafts are drafts until a person adopts them.
        personas here (guest, participant, member, steward)
        are UI presentations of authority-map roles, never
        new roles. simulated data never writes back to live
        records. every claim wears its status mark.
